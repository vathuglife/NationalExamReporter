<Window x:Class="NationalExamReporter.Views.CsvFileByYearView.DeleteCsvFileByYearView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NationalExamReporter.Views.CsvFileByYearView"
        mc:Ignorable="d"
        Title="DeleteCsvFileByYearView" Height="400" Width="600">
    <Grid>
        <DataGrid x:Name="LoadedCsvDataGrid"  Margin="10,94,10,0" CanUserAddRows="false"/>
        <Label Content="Delete a CSV" HorizontalAlignment="Left" Margin="10,16,0,0" VerticalAlignment="Top" FontSize="18" FontWeight="Bold"/>
        <Label Content="Choose one of the CSVs below, then click delete." HorizontalAlignment="Left" Margin="10,55,0,0" VerticalAlignment="Top"/>
        <Grid.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Delete" Click="DeleteCsv"/>
            </ContextMenu>
        </Grid.ContextMenu>
    </Grid>
</Window>
